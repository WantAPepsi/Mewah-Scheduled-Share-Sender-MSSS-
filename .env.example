# MARK X — local secrets only. Never commit your real .env file.
MARKETSTACK_API_KEY=

# Email is intentionally left unconfigured during timing calibration.
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
